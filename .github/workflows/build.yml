name: build test

on:
  push:
    paths-ignore:
      - '*.md'
  pull_request:
    branches: [ main ]

jobs:
  build-cuda-ubuntu-docker:
    name: build in ubuntu docker with cuda
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set Envs
        run: |
          echo "FFMPEG_VERSION=6.1" >> $GITHUB_ENV
      - name: pull base image
        id: ubuntu2004_pull
        run: |
          docker pull ubuntu:20.04
      - name: build dummy image
        run: |
          docker build -t dummy:v1 -f dummy.dockerfile .
      - name: create zip
        run: |
          docker run --rm dummy:v1 dummy.sh
          ls -l
      - name: copy zip test
        run: |
          docker cp $(docker create --name copyOutput dummy:v1):/app/dummy.zip artifacts && docker rm copyOutput

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: dummy
          path: artifacts/*